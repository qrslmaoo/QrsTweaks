# QrsTweaks (Python + PySide6)

Local Windows Optimizer (no APIs):
- Windows Optimizer
- Game Optimizer
- Password Manager

## Dev Setup
```bash
python -m venv .venv
pip install -r requirements.txt
python app/main.py
